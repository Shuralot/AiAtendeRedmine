# =================================================================
# EXEMPLO DE CONFIGURAÇÃO DE AMBIENTE (.env.example)
# Projeto: Redmine + PostgreSQL 17
#
# INSTRUÇÃO: Copie este arquivo para ".env" e preencha os valores.
# =================================================================

# -----------------------------------------------------------------
# 1. CONFIGURAÇÕES DO SERVIÇO DE BANCO DE DADOS (PostgreSQL)
# Estas variáveis são usadas pelo container 'redmine-db' para criar o banco.
# -----------------------------------------------------------------

# Nome do usuário "root" ou administrador do banco de dados
POSTGRES_USER=seu_usuario_postgres

# Senha do administrador do banco.
# Importante: Não deixe espaços em branco antes ou depois da senha.
POSTGRES_PASSWORD=sua_senha_forte_aqui

# Nome do banco de dados principal que será criado na primeira execução
POSTGRES_DB=nome_do_banco_redmine


# -----------------------------------------------------------------
# 2. CONFIGURAÇÕES DA APLICAÇÃO (Redmine)
# Estas variáveis são usadas pelo container 'redmine-app' para se conectar ao banco.
# -----------------------------------------------------------------

# Hostname do banco de dados.
# Deve ser idêntico ao nome do serviço definido no docker-compose.yml
# Se você seguiu o passo anterior, o nome é 'redmine-db'.
REDMINE_DB_POSTGRES=redmine-db

# Nome do banco de dados para conexão (Deve ser igual ao POSTGRES_DB acima)
REDMINE_DB_DATABASE=nome_do_banco_redmine

# Usuário para conexão (Deve ser igual ao POSTGRES_USER acima)
REDMINE_DB_USERNAME=seu_usuario_postgres

# Senha para conexão (Deve ser igual ao POSTGRES_PASSWORD acima)
REDMINE_DB_PASSWORD=sua_senha_forte_aqui


# -----------------------------------------------------------------
# 3. SEGURANÇA E SEGREDOS
# -----------------------------------------------------------------

# Chave secreta usada pelo Rails para criptografar cookies e sessões.
# Gere uma string aleatória longa. Pode usar: openssl rand -hex 32
REDMINE_SECRET_KEY_BASE=coloque_uma_string_aleatoria_e_longa_aqui